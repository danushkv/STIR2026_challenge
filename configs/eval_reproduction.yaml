# Offline evaluation used for the report/reproduction comparison. Stereo start
# points come from both-view annotations and are not available to the challenge
# submission harness.
tag: released_reproduction_protocol
student_checkpoint: ${oc.env:STUDENT_CHECKPOINT,./artifacts/student.pth}
val_stir_root: ${oc.env:STIR_TEST_ROOT,./data/STIRTest_2025}
test_patients: []
eval_clip_ids: []
max_eval_clips: 0
iters_list: [1, 2, 4]
out_dir: ${oc.env:EVAL_ROOT,./runs/eval_reproduction}
skip: 1
val_max_frames: 0
do_drift: true
right_queries: segs
disp_min_true: 4.0
disp_max_true: 120.0
match_lr_tol: 0.0
match_repair_ncc: 0.0
disp_repair_k: 0
disp_repair_abs: 12.0
disp_repair_mad: 6.0
